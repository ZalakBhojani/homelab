[defaults]
inventory = inventory.ini
interpreter_python = auto_silent
# Executable — pulls the sudo password from the macOS Keychain (see
# become-pass.sh for the one-time setup). -K still overrides it.
# NB: ansible-core reads this key from [defaults], not [privilege_escalation].
become_password_file = become-pass.sh
